name: us-weather-mcp
displayName: US Weather MCP Server
description: Real-time US weather checking tool using wttr.in, built with FastMCP.
version: 1.0.2
author: yunjia
homepage: https://github.com/yunjiaz7/us-weather-mcp
license: MIT
categories:
  - weather
  - data
tags:
  - weather
  - usa
  - fastmcp
  - python
runtime: container
build:
  dockerfile: Dockerfile
  dockerBuildPath: .
startCommand:
  type: stdio
  # ❌ 删掉了所有 HTTP、commandFunction、PORT 相关的复杂配置
  # ✅ 只留这一行，简单纯粹